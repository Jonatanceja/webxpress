---
// Home page CTA Section
// ------------
// Description: A centered call to action section that contains: a title, text, a user review snippet, a button and a image.

// Components
// - Layout
import Section from '../../ui/Section.astro'
import Row from '../../ui/Row.astro'
import Col from '../../ui/Col.astro'

// - UI
import { Image } from 'astro:assets'
import Button from '../../ui/Button.astro'

// Content
// - Hero Image
import heroImage from '../../../assets/monkey.webp'
---

<Section
  id="intro"
  padding="top"
  classes="bg-neutral-50 dark:bg-neutral-900 bg-top bg-cover relative md:dark:bg-[url(/planet.webp)] md:bg-[url(/planet-light.webp)] bg-fixed bg-opacity-20"
>
	<Row>
		<Col span="2" />
		<Col span="8" align="center">
			<h1>Tu <strong>sitio web</strong> en línea en días</h1>

			<p class="pb-0 text-lg">
				<strong>De la contratación al lanzamiento en 5 a 20 días hábiles.</strong> Precio cerrado desde el
				inicio, sin mensualidades.
			</p>
			<Button size="lg" link="/precios">Ver precios y plazos</Button>
		</Col>
		<Col span="2" />
		<Col span="2" />
		<Col span="8" classes='-mt-8 lg:-mt-20'>
			<Image
				src={heroImage}
				alt="Tu sitio web en órbita"
				class=""
				format="webp"
			/>
		</Col
		>
		<Col span="2" />
	</Row>
	<!--{heroBackground && <Image src={heroBackground} alt="Planeta" class="absolute top-0 left-0 w-full h-full object-cover opacity-10" format="webp" />}-->
</Section>
